import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const WorkoutHistoryScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Workout History</Text>
  </SafeAreaView>
);
