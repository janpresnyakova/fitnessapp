import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const WorkoutDetailScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Workout Detail</Text>
  </SafeAreaView>
);
