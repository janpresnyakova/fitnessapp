import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const ExerciseLibraryScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Exercise Library</Text>
  </SafeAreaView>
);
