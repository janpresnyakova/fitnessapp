import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const ExerciseStatsScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Exercise Stats</Text>
  </SafeAreaView>
);
