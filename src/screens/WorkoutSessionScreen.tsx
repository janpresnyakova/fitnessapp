import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const WorkoutSessionScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Workout Session</Text>
  </SafeAreaView>
);
