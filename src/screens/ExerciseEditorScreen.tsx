import React from 'react';
import { SafeAreaView, Text } from 'react-native';

export const ExerciseEditorScreen = (): React.JSX.Element => (
  <SafeAreaView>
    <Text>Exercise Editor</Text>
  </SafeAreaView>
);
